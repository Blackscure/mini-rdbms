# 🗄️ Mini RDBMS (Python)

A **simple relational database management system (RDBMS)** implemented in **pure Python**.  
This project demonstrates core database engine concepts such as SQL parsing, table management, indexing, joins, and an interactive REPL.

The project is **educational** in nature and focuses on understanding how relational databases work internally—without relying on existing database engines or ORMs.

## 📌 Features

- SQL-like interface
- Interactive REPL (Read–Eval–Print Loop)
- In-memory database management
- Multiple databases support
- Table creation with column data types
- CRUD operations:
  - INSERT
  - SELECT
  - UPDATE
  - DELETE
- SHOW DATABASES and SHOW TABLES
- Primary key and unique constraint support
- Basic hash-based indexing
- Simple INNER JOIN support
- Case-insensitive SQL keywords and identifiers

## 📂 Project Structure
mini_rdbms/
├── main.py
│
├── rdbms/
│ ├── init.py
│
│ ├── core/
│ │ ├── init.py
│ │ ├── database.py # Database registry and table manager
│ │ ├── table.py # Table schema, rows, constraints
│ │ └── index.py # Hash-based index implementation
│
│ ├── sql/
│ │ ├── init.py
│ │ ├── tokenizer.py # SQL tokenizer
│ │ ├── parser.py # SQL parser
│ │ └── ast.py # AST node definitions
│
│ ├── executor/
│ │ ├── init.py
│ │ ├── dispatcher.py # Routes AST nodes to executors
│ │ ├── create.py # CREATE TABLE / DATABASE execution
│ │ ├── insert.py # INSERT execution
│ │ ├── select.py # SELECT execution
│ │ ├── update.py # UPDATE execution
│ │ ├── delete.py # DELETE execution
│ │ ├── join.py # JOIN execution
│ │ ├── create_index.py # CREATE INDEX execution
│ │ └── show.py # SHOW DATABASES / SHOW TABLES
│
│ └── repl/
│ ├── init.py
│ └── shell.py # Interactive SQL REPL

## ▶️ How to Run

### Prerequisites
- Python 3.8+

### Run the REPL
```bash
python main.py

You should See

MiniRDBMS v1.0
sql>

### Supported SQL Commands
Database Management

SHOW DATABASES;
CREATE DATABASE shop;
USE shop;

Table Management

CREATE TABLE users (id INT, name TEXT);
SHOW TABLES;

Insert Data

INSERT INTO users VALUES (1, Alice);
INSERT INTO users VALUES (2, Bob);

Select Data

SELECT * FROM users;

Update Data

UPDATE users SET name = Charlie WHERE id = 2;

Delete Data

DELETE FROM users WHERE id = 1;

Indexing

CREATE INDEX ON users (id);

Join

CREATE TABLE orders (id INT, user_id INT);
INSERT INTO orders VALUES (1, 2);

SELECT * FROM users
JOIN orders
ON users.id = orders.user_id;




